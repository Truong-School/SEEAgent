# Agent server

The repository host the Agent server source code. The server functions as the centralised management system for SEEAgent

To run this server:

1. Fill in the enviroment variables in `.env.sample` and rename the file to `.env`.
2. Run `pip install -r requirements.txt` to install the dependencies.
3. Run `python3 main.py` to start the server.
