# Frontend app

The repository contains the Frontend for SEEAgent paper's human study.

To run this server:

1. Rename the `.env.sample` to `.env`.
2. Run `npm install` to install the dependencies.
3. Run `npm run dev` to start the server.
