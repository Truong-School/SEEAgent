# Game server

The repository contains the Game server source code. The server handles planning poker sessions logic and game update broadcasting.

To run this server:

1. Fill in the enviroment variables in `.env.sample` and rename the file to `.env`.
2. Run `npm install` to install the dependencies.
3. Run `npm run dev` to start the server.
